# Skill Node: ping_probe

Spec: Create a small utility that receives a host string and prints a deterministic health payload. Then persist this as a reusable skill node.

Script: /home/user/Gator/src/tools/generated/ping_probe.py
